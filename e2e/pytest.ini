[pytest]
testpaths = tests
markers =
    crud: CRUD operations (POST, GET, PUT, DELETE)
    filters: Filtering, sorting and pagination
    associations: Categories and tags endpoints
    validation: Error handling and default values
